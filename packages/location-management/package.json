{
  "name": "@opensrp/location-management",
  "version": "0.0.0",
  "main": "dist/index.js",
  "author": "OpenSRP Engineering",
  "license": "Apache-2.0",
  "private": false,
  "scripts": {
    "jest": "jest --coverage --verbose --color",
    "test": "cd ../../ && jest test  packages/keycloak-user-management --verbose --collectCoverage=true && cd packages/keycloak-user-management",
    "test:watch": "cd ../../ && jest test  packages/keycloak-user-management --verbose --watch && cd packages/keycloak-user-management",
    "tsc": "tsc",
    "lint": "eslint './**/*.{js,jsx,ts,tsx}'",
    "transpile": "babel src -d dist --root-mode upward --extensions '.ts,.tsx' --ignore '**/*.test.ts,**/*.test.tsx,**/tests,**/__tests__'"
  },
  "files": [
    "dist"
  ],
  "jest": {
    "automock": false,
    "setupFiles": [
      "../../setupTests"
    ]
  },
  "peerDependencies": {
    "react": "^16.13.1"
  }
}
